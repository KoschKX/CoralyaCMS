// Re-exported from the shared editor utilities so block-agnostic UI components
// (e.g. ColToolbar) can import FRACTION_PRESETS without depending on this block.
export { FRACTION_PRESETS } from "@/lib/editor/col-width";
export type { FractionPreset } from "@/lib/editor/col-width";
